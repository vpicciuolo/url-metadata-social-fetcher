import {describe,expect,it} from 'vitest';import {assertSafeUrl,isSafeUrl,normalizeUrl} from '../src/core/url-guard.js';
describe('assertSafeUrl',()=>{it('accepts public https URLs',()=>expect(assertSafeUrl('https://example.com/page').hostname).toBe('example.com'));it.each(['http://localhost/admin','http://127.0.0.1:8080/','http://169.254.169.254/latest/meta-data/','http://10.0.0.5/','http://192.168.1.1/','http://[::1]/','file:///etc/passwd','ftp://example.com/file','https://user:pass@example.com/','http://metadata.google.internal/','http://service.local/'])('blocks %s',(url:string)=>expect(isSafeUrl(url)).toBe(false));it('honours allow/deny lists',()=>{expect(isSafeUrl('https://evil.com',{allowHosts:['example.com']})).toBe(false);expect(isSafeUrl('https://cdn.example.com',{allowHosts:['example.com']})).toBe(true);expect(isSafeUrl('https://example.com',{denyHosts:['example.com']})).toBe(false);});});
describe('normalizeUrl',()=>{it('strips tracking params, www and trailing slash',()=>expect(normalizeUrl('https://WWW.Example.com/path/?utm_source=x&id=7#top')).toBe('https://example.com/path?id=7'));});
