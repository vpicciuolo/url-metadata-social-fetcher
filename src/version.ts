/** Human-facing repository release version. */
export const RELEASE_VERSION = '1.0.0.1' as const;

/** npm requires three-component Semantic Versioning, so the package manifest remains 1.0.0. */
export const PACKAGE_VERSION = '1.0.0' as const;
export const PROJECT_NAME = 'url-metadata-social-fetcher' as const;
